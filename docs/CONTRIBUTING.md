# 👨‍💻

 Operit ！.

## Translated section

### 📜

Operit TypeScript/JavaScript AI . [ (SCRIPT_DEV_GUIDE.md)](./SCRIPT_DEV_GUIDE.md).

### 🔌 MCP

 MCP AI ,、. Operit AI zip .

## Operit

 Operit AI ,.

### 🛠️

, [ (BUILDING.md)](./BUILDING.md) Android .

### 🚀

1. ****: [Issue ](https://github.com/AAswordman/Operit/issues) ,****.
2. ****: ,****,.
3. ****: ****,.
4. ****: ,.

### 🎨

....！

- commit ""
- ,
- translated

### 🔄

,:

1. ****:
 - Fork Clone .
 - : `git remote add upstream https://github.com/AAswordman/Operit.git`

2. ****:
 - `main` `pr-branch` .
      ```bash
      git fetch upstream
      git checkout main
      git merge upstream/main
      git checkout pr-branch
      git merge main
      ```
 - `pr-branch` .
      ```bash
      git checkout -b feature/your-feature-name
      ```

3. ****:
 - ,** `main` **. `rebase` .
      ```bash
      git fetch upstream
 git rebase upstream/main # git merge upstream/main
      ```
 - ,.
      ```bash
 # rebase , --force
      git push origin feature/your-feature-name --force
      ```

4. ** Pull Request**:
 - GitHub, Pull Request,** `pr-branch`**.

### ⚠️

- **,**,.
- ** PR `pr-branch` **.
- ** PR , `main` **,.
- PR .

---

！ PR、Issue Operit .
> ****: .！

---

## Translated section

 Operit AI .,:

1. **、( GitHub, GitLab, Gitee ).** ,,、,“”.
2. **.** ,.

、.